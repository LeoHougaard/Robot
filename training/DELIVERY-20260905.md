# Robot delivery work record

Leo confirmed tomorrow's first test is slow walking and turning on floors, carpet and small uneven surfaces. Outdoor terrain, slopes and steps remain later goals. The robot, Pixel, battery, mounting and carried weight match the August 29 capture. Leo alone initiates physical motion. The ESP32 returns immediately before the test; physical 50 Hz communication cannot be measured tonight. Source changes, training and prudent GitHub commits/pushes are authorized.

## Plan and evidence

1. Preserve the strongest existing artifacts and measure their actual behavior. **Done:** V18 epoch 970 and its input hashes are preserved remotely. Corrected deterministic command progression and camera recording expose negligible motion and a persistently airborne foot. It is rejected for walking. V1 and the deployed V2 actor remain preserved.
2. Correct physical actuator/sensor assumptions and training throughput. **Implemented and checked:** the acceleration-limited motor-coordinate response beats the first-order fit on the reserved capture window. Convex collision overrides preserve all authored attributes except the 29 collision approximation settings. Matched 128-environment benchmarks improved from 38.9 to 776 transitions/s. The original SDF asset remains available for contact-fidelity evaluation.
3. Train bounded candidates, inspect exact-checkpoint videos and compare fixed deterministic screens. **Epochs 100 and 300 rejected:** V20 epoch 100 completed in 1,016 seconds with 128 environments. Both it and its starting actor completed all 21 flat-screen segments and exact 103-second movies. Forward speed changed from 0.0166 to 0.0233 m/s, but reverse changed from +0.0022 to +0.0194 m/s (wrong direction) and pitch worsened. The continuation reached total epoch 300 with the same training objective in another 2,013 seconds. Its first launch stalled in native Isaac startup; a container restart recovered it without changing the checkpoint. Completed run: `20260905T062729Z-train-94`. Its complete 21-segment flat screen and 103-second movie show a further regression and a persistently airborne front foot. Forward speed is -0.0065 m/s for a +0.08 request. The sampled movie shows a largely fixed tilted pose across commands. No policy was promoted.
4. Verify the hardware software path, exact observation/action contract and export provenance. **Software checks passed; hardware pending:** firmware 0.1.15 builds; its deadline scheduler and production policy-command handlers pass native tests. Pixel 0.2.9 is installed, its 51 unit tests pass, and installed actor parity and service/six-command API checks pass on the actual phone. The hardware diagnostic is deliberately skipped unless explicitly invoked with the torque-off test argument. Raw firmware sensor fixtures pass through the production Kotlin preprocessing and match Python observations, including wrapping timestamps and current loss. Export requires exact actor/source/console/video hashes, separate suites, visual decisions and original-SDF fidelity evidence; no candidate has satisfied those gates. The browser layout was inspected; the native screen was behind the phone's lock screen.
5. Preserve installable artifacts, push verified milestones, and perform motor-disabled transport tests when the board returns. **Pending hardware availability.**

## Experiment contract

V20 is a separate 426-input family with history indices `[0,10,20,21,22,23]`, actor and critic each `[128,128,128]`, a new optimizer/critic and a 50 Hz sampled observation contract. The first experiment initialized its actor from the exact deployed V2 ONNX function, copied into the new layout with new columns zeroed. Independent ONNX-versus-PyTorch comparison passed 100 cases with maximum action difference 2.44e-6. This is actor initialization, not recovery of the original PPO checkpoint.

Training uses forward commands up to 0.10 m/s, lateral up to 0.08 m/s, yaw up to 0.30 rad/s, small posture commands and a fixed mild terrain distribution. It keeps body tracking dominant and the diagonal prior subordinate to actual progress. It has no self-righting resets. The fixed screen in `delivery_contract.py` tests forward/reverse 0.08 m/s, lateral 0.06 m/s, turns 0.20 rad/s, combined commands, stopping, posture and 20% current dropout. The initialized actor and trained candidates must face the same screen. Passing requires signed progress, proportional slow-command tracking, four-foot contact evidence, stability, finite complete metrics and no resets. Numeric results and actual video both control promotion. Wider commands are not certified by this initial screen.

The untrained transferred actor cycles some legs at a 0.18 m/s diagnostic request but achieves only about 0.006 m/s forward and moves the wrong way in reverse. That diagnostic establishes why copying the old actor alone is insufficient; it is not a passing baseline.

Run: `/home/leo/isaac-workspace/projects/training/runs/simple_dog/20260905T051951Z-train-838`.
Profile canonical SHA: `2e249f1a8efc7df0fca72dfaf2e50712a2b108874bdf7a883cbd69f02b25ca0d`.
Initialized checkpoint SHA: `ce0fa651d220b28cd67bfd3a93f35f37065f28d2ba8386dfc04c01443ded38ef`.
Each run executes an immutable source copy with a source/input hash manifest. Generated evidence and checkpoints remain on the GB10 under `training/reviews/20260905-delivery` and the run directories.

## Failure ledger and decisions

Epoch 300 checkpoint SHA: `6b92dc94169815d2ab4877e985545b4ff957d6b2fd1a24dcd15c5fc4a85f57a6`.
Epoch 300 evidence: `training/logs/rl_games/quadruped_current_body_v20_assembly_four_leg_linkage_12dof/2026-09-05_06-27-44/evaluation/20260905T070720.691663Z-deliveryflat-seed42`. Player exit 0; video SHA `78088225d04b2d725a59056c62a7627944b48301332ee404cb783c5e19f04c56`; exact actor/movie visual rejection recorded.

Epoch 100 checkpoint SHA: `4223c43dff24d053cfbfe79d9a359b89b6f1fc4c30caef0a2c7ef6e04f0b8ac0`.
Full evaluation: `training/logs/rl_games/quadruped_current_body_v20_assembly_four_leg_linkage_12dof/2026-09-05_05-20-06/evaluation/20260905T055711.027264Z-deliveryflat-seed42`.

| Category | Observation | Hypothesis and next decision |
|---|---|---|
| Motion without useful tracking | Forward 0.08 m/s request produces 0.0233 m/s; reverse -0.08 produces +0.0194, the wrong direction. | Early actor transfer retains a largely command-independent gait. The epoch-300 continuation regressed and largely parked on three feet. After correcting height measurement, test a fresh actor without changing the remaining objective or PPO settings. |
| Attitude and contact | About 0.20–0.25 rad nose-down pitch, asymmetric swing fractions, excessive stance slip. The exact movie shows repeated leg cycling and rocking. | Slow actuator dynamics change the useful gait. Attitude reward balance or state estimation may need revision if the matched continuation stalls. This is a hypothesis, not established causation. |
| Misleading terrain coverage | A one-environment evaluation of the training mixture selects its first, flat column. | Corrected the mild suite to an explicit held-out 1–6 mm uneven surface. Slopes and stairs are not certified by that suite. |
| Incomplete rendered evaluation | The original 600-second player budget expired before all 21 segments and video finalization. | Use 1,200 seconds and 640×360 recording. Preserve incomplete runs as rejected evidence. |

The export path recomputes the fixed numeric gates from preserved logs and binds each visual decision to the exact movie and checkpoint. Source snapshots also hash referenced asset layers. The firmware encoder angle conversion uses 4095 in its reported-degree convention; simulation now matches that convention. This small correction occurred after the first 100 training epochs and is present in both matched flat evaluations.

## Remaining uncertainty

The read-only diagnostic and supervised test procedure are in [DELIVERY-TEST.md](../pixel_robot/docs/DELIVERY-TEST.md). Its firmware path verifies torque is already off and cannot write motor positions. It measures sensors, inference, recording and USB, excluding the actuation broadcast and motion-dependent loading. No physical 50 Hz result exists yet.

A separate test-only 426-input ONNX fixture passed the actual Pixel loader and XNNPACK inference path at scheduled 20 ms intervals: 500 measured calls after 50 warmups, median 1.831 ms, p99 4.701 ms, maximum 7.818 ms. All eight reference vectors passed throughout the test, and the installed V2 actor passed again. The random fixture exists only in the instrumentation APK and cannot be treated as a walking policy. Exact fixture SHA: `24b0aafc5f923664ebed6c4a8ccd259f127b72c9ff185bffdf83e2a5ebb7d44e`. Report: laptop `pixel_robot/delivery-onnx-fixture-result.json` and app `files/delivery-onnx-fixture-result.json`. This does not replace exact trained-actor parity or the motor-disabled transport test.

An additional geometry check found authored mass totals 1.616 kg and the neutral CAD foot-COM height reference is 0.1412 m. Standing preflight root height is about 0.1817 m. The inherited posture metric measured height above the two lowest foot-link centers of mass, not the ground. V20 now measures clearance above 25 vertical terrain rays under a 30 cm square chassis footprint, with a nominal 18 cm root height. This is reward/evaluation data only; the 426-input actor receives no terrain rays. Missing ground fails the measurement. Four CPU checks pass. A fresh Isaac process on uneven terrain verified all 25 rays spanning 1?6 mm, following each robot, with 426 finite actor inputs. Zero joint offsets settle at 0.18157 m clearance with roll/pitch about 0.0001/0.0002 rad. A first attempt reused the flat terrain ray mesh across two scene instances; each terrain now runs in a separate process, as production evaluations do. The separate flat check also passed: terrain height is zero within 3e-8 m, standing clearance is 0.18178 m, and roll/pitch are about 0.00012/0.000004 rad. Evidence: `training/reviews/20260905-delivery/ground-height-{flat,uneven}-v4.json`. Earlier evaluations retain their exact old source and are not accepted under the corrected metric. The preflight's 11–12 N foot readings are transient maxima, not static supported weight.

The active optimization trial starts from a preserved random actor, critic and optimizer rather than the transferred V2 gait. This is separate from the height correctness fix; it does not establish that height caused the failed gait. Random initialization SHA: `86fd7ab318221c053d48a448efd870d680b90d26292b2947d9e0c80d8c805972`, with exact initializer/config archived beside it under `random-initialization/nn`. Keep rewards, actuator dynamics, commands and PPO settings fixed for this comparison. Run `20260905T072756Z-train-2239`, experiment `2026-09-05_07-28-10`, started with a 100-epoch target and no other objective or PPO changes. Startup and the first epochs are healthy at about 800 transitions/s. Evaluate the saved random baseline and exact trained actor under the corrected source before continuation.

The capture identifies effective encoder response under its recorded load, not continuous torque limits, contact geometry, ground truth body velocity or voltage dependence. The knee/hip coordinate relationship is retained; motor housing attachment remains a mechanical question before changing generalized torque coupling. Convex contact fidelity must be compared with the preserved SDF model. Real-world walking is unverified until the supervised physical test.

Research and historical intent: see [the review](SIM-TO-REAL-REVIEW-20260904.md). The approach follows measured actuator and delay modeling in [Tan et al.](https://arxiv.org/abs/1804.10332) and [Hwangbo et al.](https://arxiv.org/abs/1901.08652), with bounded parallel PPO and curriculum principles from [Rudin et al.](https://arxiv.org/abs/2109.11978). These papers motivate the method; they do not establish transfer for this robot.
