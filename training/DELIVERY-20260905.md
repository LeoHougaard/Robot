# Robot delivery work record

Leo confirmed tomorrow's first test is slow walking and turning on floors, carpet and small uneven surfaces. Outdoor terrain, slopes and steps remain later goals. The robot, Pixel, battery, mounting and carried weight match the August 29 capture. Leo alone initiates physical motion. The ESP32 returns immediately before the test; physical 50 Hz communication cannot be measured tonight. Source changes, training and prudent GitHub commits/pushes are authorized.

## Plan and evidence

1. Preserve the strongest existing artifacts and measure their actual behavior. **Done:** V18 epoch 970 and its input hashes are preserved remotely. Corrected deterministic command progression and camera recording expose negligible motion and a persistently airborne foot. It is rejected for walking. V1 and the deployed V2 actor remain preserved.
2. Correct physical actuator/sensor assumptions and training throughput. **Implemented and checked:** the acceleration-limited motor-coordinate response beats the first-order fit on the reserved capture window. Convex collision overrides preserve all authored attributes except the 29 collision approximation settings. Matched 128-environment benchmarks improved from 38.9 to 776 transitions/s. The original SDF asset remains available for contact-fidelity evaluation.
3. Train bounded candidates, inspect exact-checkpoint videos and compare fixed deterministic screens. **Epochs 100 and 300 rejected:** V20 epoch 100 completed in 1,016 seconds with 128 environments. Both it and its starting actor completed all 21 flat-screen segments and exact 103-second movies. Forward speed changed from 0.0166 to 0.0233 m/s, but reverse changed from +0.0022 to +0.0194 m/s (wrong direction) and pitch worsened. The continuation reached total epoch 300 with the same training objective in another 2,013 seconds. Its first launch stalled in native Isaac startup; a container restart recovered it without changing the checkpoint. Completed run: `20260905T062729Z-train-94`. Its complete 21-segment flat screen and 103-second movie show a further regression and a persistently airborne front foot. Forward speed is -0.0065 m/s for a +0.08 request. The sampled movie shows a largely fixed tilted pose across commands. No policy was promoted.
4. Verify the hardware software path, exact observation/action contract and export provenance. **Software checks passed; hardware pending:** firmware 0.1.15 builds; its deadline scheduler and production policy-command handlers pass native tests. Pixel 0.2.9 is installed, its 51 unit tests pass, and installed actor parity and service/six-command API checks pass on the actual phone. The hardware diagnostic is deliberately skipped unless explicitly invoked with the torque-off test argument. Raw firmware sensor fixtures pass through the production Kotlin preprocessing and match Python observations, including wrapping timestamps and current loss. Export requires exact actor/source/console/video hashes, separate suites, visual decisions and original-SDF fidelity evidence; no candidate has satisfied those gates. The browser layout was inspected; the native screen was behind the phone's lock screen.
5. Preserve installable artifacts, push verified milestones, and perform motor-disabled transport tests when the board returns. **Pending hardware availability.**

## Experiment contract

V20 is a separate 426-input family with history indices `[0,10,20,21,22,23]`, actor and critic each `[128,128,128]`, a new optimizer/critic and a 50 Hz sampled observation contract. The first experiment initialized its actor from the exact deployed V2 ONNX function, copied into the new layout with new columns zeroed. Independent ONNX-versus-PyTorch comparison passed 100 cases with maximum action difference 2.44e-6. This is actor initialization, not recovery of the original PPO checkpoint.

Training uses forward commands up to 0.10 m/s, lateral up to 0.08 m/s, yaw up to 0.30 rad/s, small posture commands and a fixed mild terrain distribution. It keeps body tracking dominant and the diagonal prior subordinate to actual progress. It has no self-righting resets. The fixed screen in `delivery_contract.py` tests forward/reverse 0.08 m/s, lateral 0.06 m/s, turns 0.20 rad/s, combined commands, stopping, posture and 20% current dropout. The initialized actor and trained candidates must face the same screen. Passing requires signed progress, proportional slow-command tracking, four-foot contact evidence, stability, finite complete metrics and no resets. Numeric results and actual video both control promotion. Wider commands are not certified by this initial screen.

The untrained transferred actor cycles some legs at a 0.18 m/s diagnostic request but achieves only about 0.006 m/s forward and moves the wrong way in reverse. That diagnostic establishes why copying the old actor alone is insufficient; it is not a passing baseline.

Run: `/home/leo/isaac-workspace/projects/training/runs/simple_dog/20260905T051951Z-train-838`.
Profile canonical SHA: `2e249f1a8efc7df0fca72dfaf2e50712a2b108874bdf7a883cbd69f02b25ca0d`.
Initialized checkpoint SHA: `ce0fa651d220b28cd67bfd3a93f35f37065f28d2ba8386dfc04c01443ded38ef`.
Each run executes an immutable source copy with a source/input hash manifest. Generated evidence and checkpoints remain on the GB10 under `training/reviews/20260905-delivery` and the run directories.

## Failure ledger and decisions

Epoch 300 checkpoint SHA: `6b92dc94169815d2ab4877e985545b4ff957d6b2fd1a24dcd15c5fc4a85f57a6`.
Epoch 300 evidence: `training/logs/rl_games/quadruped_current_body_v20_assembly_four_leg_linkage_12dof/2026-09-05_06-27-44/evaluation/20260905T070720.691663Z-deliveryflat-seed42`. Player exit 0; video SHA `78088225d04b2d725a59056c62a7627944b48301332ee404cb783c5e19f04c56`; exact actor/movie visual rejection recorded.

Epoch 100 checkpoint SHA: `4223c43dff24d053cfbfe79d9a359b89b6f1fc4c30caef0a2c7ef6e04f0b8ac0`.
Full evaluation: `training/logs/rl_games/quadruped_current_body_v20_assembly_four_leg_linkage_12dof/2026-09-05_05-20-06/evaluation/20260905T055711.027264Z-deliveryflat-seed42`.

| Category | Observation | Hypothesis and next decision |
|---|---|---|
| Motion without useful tracking | Forward 0.08 m/s request produces 0.0233 m/s; reverse -0.08 produces +0.0194, the wrong direction. | Early actor transfer retains a largely command-independent gait. The epoch-300 continuation regressed and largely parked on three feet. After correcting height measurement, test a fresh actor without changing the remaining objective or PPO settings. |
| Attitude and contact | About 0.20–0.25 rad nose-down pitch, asymmetric swing fractions, excessive stance slip. The exact movie shows repeated leg cycling and rocking. | Slow actuator dynamics change the useful gait. Attitude reward balance or state estimation may need revision if the matched continuation stalls. This is a hypothesis, not established causation. |
| Misleading terrain coverage | A one-environment evaluation of the training mixture selects its first, flat column. | Corrected the mild suite to an explicit held-out 1–6 mm uneven surface. Slopes and stairs are not certified by that suite. |
| Incomplete rendered evaluation | The original 600-second player budget expired before all 21 segments and video finalization. | Use 1,200 seconds and 640×360 recording. Preserve incomplete runs as rejected evidence. |

The export path recomputes the fixed numeric gates from preserved logs and binds each visual decision to the exact movie and checkpoint. Source snapshots also hash referenced asset layers. The firmware encoder angle conversion uses 4095 in its reported-degree convention; simulation now matches that convention. This small correction occurred after the first 100 training epochs and is present in both matched flat evaluations.

## Remaining uncertainty

The read-only diagnostic and supervised test procedure are in [DELIVERY-TEST.md](../pixel_robot/docs/DELIVERY-TEST.md). Its firmware path verifies torque is already off and cannot write motor positions. It measures sensors, inference, recording and USB, excluding the actuation broadcast and motion-dependent loading. No physical 50 Hz result exists yet.

A separate test-only 426-input ONNX fixture passed the actual Pixel loader and XNNPACK inference path at scheduled 20 ms intervals: 500 measured calls after 50 warmups, median 1.831 ms, p99 4.701 ms, maximum 7.818 ms. All eight reference vectors passed throughout the test, and the installed V2 actor passed again. The random fixture exists only in the instrumentation APK and cannot be treated as a walking policy. Exact fixture SHA: `24b0aafc5f923664ebed6c4a8ccd259f127b72c9ff185bffdf83e2a5ebb7d44e`. Report: laptop `pixel_robot/delivery-onnx-fixture-result.json` and app `files/delivery-onnx-fixture-result.json`. This does not replace exact trained-actor parity or the motor-disabled transport test.

An additional geometry check found authored mass totals 1.616 kg and the neutral CAD foot-COM height reference is 0.1412 m. Standing preflight root height is about 0.1817 m. The inherited posture metric measured height above the two lowest foot-link centers of mass, not the ground. V20 now measures clearance above 25 vertical terrain rays under a 30 cm square chassis footprint, with a nominal 18 cm root height. This is reward/evaluation data only; the 426-input actor receives no terrain rays. Missing ground fails the measurement. Four CPU checks pass. A fresh Isaac process on uneven terrain verified all 25 rays spanning 1?6 mm, following each robot, with 426 finite actor inputs. Zero joint offsets settle at 0.18157 m clearance with roll/pitch about 0.0001/0.0002 rad. A first attempt reused the flat terrain ray mesh across two scene instances; each terrain now runs in a separate process, as production evaluations do. The separate flat check also passed: terrain height is zero within 3e-8 m, standing clearance is 0.18178 m, and roll/pitch are about 0.00012/0.000004 rad. Evidence: `training/reviews/20260905-delivery/ground-height-{flat,uneven}-v4.json`. Earlier evaluations retain their exact old source and are not accepted under the corrected metric. The preflight's 11–12 N foot readings are transient maxima, not static supported weight.

The active optimization trial starts from a preserved random actor, critic and optimizer rather than the transferred V2 gait. This is separate from the height correctness fix; it does not establish that height caused the failed gait. Random initialization SHA: `86fd7ab318221c053d48a448efd870d680b90d26292b2947d9e0c80d8c805972`, with exact initializer/config archived beside it under `random-initialization/nn`. Keep rewards, actuator dynamics, commands and PPO settings fixed for this comparison. Run `20260905T072756Z-train-2239`, experiment `2026-09-05_07-28-10`, started with a 100-epoch target and no other objective or PPO changes. Startup and the first epochs are healthy at about 800 transitions/s. Evaluate the saved random baseline and exact trained actor under the corrected source before continuation.

## Fresh actor result and velocity-reward experiment

The fresh trial completed 100 epochs. Its checkpoint SHA is `823d7537fb770997d30e240c1eef6e027c51e1d39edeaea09694b93883dd1227`. All 21 flat segments and the 103-second movie completed, with player exit 0 and no falls. Forward speed was 0.0025 m/s for +0.08 requested; reverse was -0.0001 m/s for -0.08 requested. The overview and dense 4.00–5.76 second frames show rocking over asymmetric support rather than a useful stride. It is rejected. Evidence: `2026-09-05_07-28-10/evaluation/20260905T074602.739357Z-deliveryflat-seed42`; video SHA `2de0d6fb2cb4aae2dddc7891cd6c152c4cda0be660710b053780ce0d230e5da4`.

The identical starting actor stands nearly level and still: stand pitch 0.0032 rad, roll 0.0094 rad, forward speed 0.0003 m/s and mean absolute lateral speed 0.0011 m/s. It does not walk. Training added motion without useful progress.

A direct test of the production reward found a competing zero-net-motion strategy. With the diagonal bonus disabled and posture/yaw held perfect, the old formula gives 0.06164 reward per step for parking at a +0.08 m/s request. Moving forward at +0.08 for 75% of the time and backward at -0.24 for 25% gives 0.08750 despite zero net travel. This is a synthetic arithmetic test, not a physical rollout. It establishes a reward loophole, not proof that it is the only cause of the learned gait.

The proposed correction uses squared planar/yaw tracking costs, preserves credit limits for overspeed but charges the full wrong-way motion, and gives wrong-way motion a larger shortfall cost. The diagonal prior remains capped at 0.25 and gated by progress; a quadratic gate and command-dependent cap keep small oscillations from earning more style reward than their tracking cost. Signed training-distance diagnostics also stop discarding backward travel. Correct steady tracking retains its previous optimum. The same synthetic test now gives 0.05160 for parking and -0.06360 for the zero-net asymmetric cycle. Six CPU tests cover stops, signed tracking, symmetric/asymmetric zero-net translation and turning, mixed commands, overspeed, settling and falls.

The next trial uses identical random initial weights, seed 42, 128 environments and 100 epochs, with only this velocity-reward change. The paired evaluations completed before launch. Keep the physical model, observations, commands and fixed acceptance gates unchanged. The lower rewards are not comparable with the old objective; compare actual motion, attitude, contact evidence and video. Watch termination rates because the new tracking losses can be negative.

Two launches (`20260905T081140Z-train-3571` and `20260905T082106Z-train-113`) stalled before the first simulation step. A captured Python stack ends inside native `SimulationApp._start_app`; there was no GPU workload or PPO progress. Restarting the task container alone did not recover the second attempt. The next startup test limits OpenBLAS, OpenMP and MKL to one CPU thread in this process only. Exact original run arguments and execution settings are preserved in `training/reviews/20260905-delivery/startup-thread-limit.json`. Isaac Lab's [current launcher source](https://isaac-sim.github.io/IsaacLab/develop/_modules/isaaclab/app/app_launcher.html) describes a NumPy/OpenBLAS at-fork conflict during Kit startup. That is a candidate explanation, not a proven diagnosis of this stall. No host services or library versions changed.

The thread-limited retry reached PPO: run `20260905T082522Z-train-55`, experiment `2026-09-05_08-25-37`, about 800 transitions/s initially. The source manifests differ from the prior fresh 100-epoch control only in `simple_dog_task_current_body_v20/env.py`; all profile, fit, starting-checkpoint and asset inputs match exactly. The first completed episodes remain 599 steps and losses are finite. V20 training and evaluation now set these process-local thread limits and record them in new evidence manifests. Seven evaluation/provenance tests and Python/bash syntax checks pass.

This recovery invocation used the container's default working directory, so the active RL-Games experiment is temporarily under `/workspace/isaaclab/logs/rl_games`. A bounded supervisor waits for this exact training process to complete, then copies the complete experiment to the authoritative mounted `training/logs/rl_games` directory and verifies every file hash before starting the flat evaluation. Script, status and copy manifest are under `training/reviews/20260905-delivery` with the `velocity-trial` prefix. It refuses an existing destination or an occupied GPU. The normal repository launcher retains its intended working directory.

The retry saved epoch 100 at frame 819,200, but its launcher failed afterward. I deployed the updated `run_simple_dog.sh` while its original shell was still waiting for training; when the child returned, Bash read changed file bytes, attempted a malformed command and overwrote `console.log`. The original failed status/exit 127 is retained. TensorBoard, resolved configurations, immutable Python source and all checkpoints survived. Both final epoch-100 files contain finite model tensors. The selected canonical checkpoint is `last_quadruped_current_body_v20_assembly_four_leg_linkage_12dof_ep_100_rew_15.720187.pth`, SHA `f87d0a48f0974041337e9be3c5bb8bc4a7870bf042ee101a587d49ad1b905b70`. Every copied experiment file was hash-verified into the authoritative mounted directory; the copy manifest records the launcher failure. The flat evaluator was then started explicitly after verifying an idle GPU. Its own logs and provenance remain separate and complete collection is still required.

The launcher now immediately executes a unique read-only copy of itself, preserves that script/hash beside the run, and keeps its arguments and working directory. A regression test runs the production bootstrap, replaces the original source while the shell waits, then verifies its original completion, arguments, working directory and exact frozen contents. That test and bash syntax pass. Future deployments cannot change a running launcher's unread instructions.

The corrected-reward epoch 100 completed its entire flat evaluation with player exit 0 and 5,150 video frames. Evidence: `2026-09-05_08-25-37/evaluation/20260905T084605.667824Z-deliveryflat-seed42`; video SHA `44778c21997ae14205d0801287d24845e0a064283ecd21babdb1de3328645cb2`. It failed 213 checks. Forward speed is -0.0091 m/s for +0.08 requested, with roll error 0.2190 rad and pitch error 0.1351 rad. The overview and dense 4.00-6.40 second frames show rocking and an asymmetric extended-leg pose, without a useful forward stride. Exact-hash visual rejection is saved. This is not an improvement over the prior fresh 100-epoch actor.

Before a longer training window, an unloaded, fixed-body diagnostic measured material points on the neutral foot soles, using the actual collision meshes, joint limits and servo trajectories. All 49 joint-pose cases settled within 0.000924 rad of their targets, with root drift below 5e-9 m. The local Jacobian estimate for 0.08 m/s horizontal foot motion uses about 64.6% of the front motor speed limit and 34.6% at the rear. It does not include acceleration, stance/contact constraints, or loaded periodic walking and cannot certify gait feasibility. It gives no basis to lower the fixed command screen. Evidence and exact executed source are preserved under `foot-tip-reach-v2.*` in the review directory. The first attempt hit Isaac's unsupported generic fixed-root conversion for a container Xform; the successful diagnostic creates a world joint to the explicitly profiled base body inside the disposable simulation scene. It does not change the robot asset or training physics.

The next bounded window continues this corrected objective to epoch 500, keeping its current checkpoint, physical model, observation contract, commands, PPO settings and acceptance screen. The first 100 epochs cover only 819,200 transitions. A longer unchanged trial will distinguish slow acquisition from another persistent failure without stacking another reward or critic change. The new checkpoint remains an exploratory candidate, not the retained best or an exportable policy.

## Epoch 500 result and training-only critic comparison

Run `20260905T090717Z-train-1207` completed epoch 500 with exit 0 after 4,057.96 additional training seconds. Its immutable launcher preserved the full console and completion status. Selected checkpoint SHA: `938018de6e39f5c627ffd41d1c44351412fc5b576a7cf52afd14b11899fa67a5`, under experiment `2026-09-05_09-07-32`. The complete flat evaluation `20260905T161443.418866Z-deliveryflat-seed42` finished with player exit 0, 240 failed checks and no resets. Forward speed is 0.0006 m/s for 0.08 requested; reverse is -0.0178 m/s for -0.08 requested. Forward tilt averages 0.3233 rad. The exact 5,150-frame movie and dense stride frames show rocking, large lean and uneven support. Video SHA: `97338033347efae137f3f7d28ce3e71a420dee2cf373f176b268c4a1973045af`. Visual rejection is preserved. The longer run did not establish a useful gait.

The next controlled change adds a training-only value network with 436 inputs: the unchanged 426 actor observations plus true body linear velocity, ground-relative height, roll/pitch and four contact flags. This tests whether the partially observed value estimate is obstructing learning; it does not establish that as the cause. Rewards, commands, terrain, actuator/sensor models and actor architecture remain fixed. The original random epoch-zero actor, all its model tensors and optimizer are preserved exactly. The independent critic starts fresh. New initialization SHA: `9073e5a0cf9840178be9da0f9dcd7baeb637c165295a882fea68ad807cfc7572`, under `privileged-initialization/nn`; exact initializer and configuration are archived beside it. The real RL-Games critic restores and evaluates 128 CPU cases. Eleven CPU tests check reward behavior, ground height, privileged layout and actor-input isolation.

Initial launch `20260905T163401Z-train-1952` stalled inside native Kit startup with the thread limits already set. Those limits alone do not guarantee startup recovery. After restarting only the idle task container, run `20260905T163823Z-train-114`, experiment `2026-09-05_16-38-37`, reached PPO and completed its first updates, constructing the expected 426-input actor and 436-input central value network. This is a fresh 500-epoch comparison. The installed RL-Games version serializes central critic model/statistics but not its separate optimizer, so this comparison uses one uninterrupted training window; a later continuation must account for that limitation.

Leo requested less frequent monitoring. Check roughly every 15-20 minutes after verifying startup. The bounded supervisor `training/reviews/20260905-delivery/evaluate-privileged500b.py` records live status, detects missing/stalled training, and starts the fixed flat evaluation after successful completion and an idle-GPU check. It cannot promote a policy. Inspect the exact final video and numerical results before choosing another run or export.

The privileged-critic run completed all 500 epochs with exit 0. Checkpoint SHA: `88306775268f7b87b3d33f12809c4b00db0e4ee0d67751ba5235a48213859d23`. Its flat evaluation `20260905T180436.058872Z-deliveryflat-seed42` completed all 21 segments with player exit 0 in 683.22 seconds. The evaluator returned 1 because 217 acceptance checks failed. Forward speed is -0.0010 m/s for +0.08 requested, mean tilt 0.238 rad, and the rear-right foot's airborne fraction is 0.956 during forward motion. The 5,150-frame movie SHA is `1d56ff6cec8a018fafdcfae36c3fac95fa2a238a35fdfa42ff2ed63b511e5ce9`. Overview and dense 4.00-6.40 second frames show rocking over uneven support without a useful alternating stride. Exact-hash visual rejection is saved. The extra critic inputs have not established a useful gait, and this checkpoint is not exported.

Leo requested videos for his own verification. Both complete, unedited 500-epoch comparison movies and their measurement JSON are copied to `C:\Users\Leo\Videos\Robot-policy-review-20260905` on the desktop and laptop; both copies match their original evidence hashes. `index.html` provides synchronized, initially paused/muted playback, slow motion, all 21 command jumps and measured movement. The desktop also serves this review on loopback port 8875. Browser checks verified both 103-second movies decode and the command selector seeks both videos to the forward target at 3 seconds. Browser screenshot capture failed; the actual rollout contact sheets were inspected separately. These videos show rejected exploratory policies, not a deployable walking result.

Before another long PPO window, `probe_delivery_stepping.py` tested a zero-action control and 36 simple diagonal stepping patterns using the measured sole Jacobians, the unchanged V20 servo/control path, and the causal IMU gravity estimate. This is a simulation diagnostic, not a trained policy or promotion screen. Evidence: `training/reviews/20260905-delivery/stepping-v1`. All runs last 20 seconds and measure the last 14 seconds. The zero-action case remains level and stationary. The 0.04 m/s, 0.6 Hz, 20 mm lift, 0.5 attitude-gain case achieved 0.0405 m/s mean forward speed, 0.0384 m/s mean absolute lateral speed and 0.0787 rad mean tilt, with all feet landing and no resets or requested joint clipping. This demonstrates coordinated travel in the current model, with substantial lateral sway; it does not establish hardware feasibility or passing locomotion. The separate single-environment recording under `stepping-video-v1` completed with matching motion (0.0406 m/s forward, 0.0384 m/s mean absolute lateral speed, 0.0783 rad tilt). The 1,000-frame movie SHA is `6879cae6c10b993406d99edac2bf95b853f5eaf24564c732a55479ea504a32f2`. Its overview and dense 8.00-9.80 second frames show alternating stepping and travel over the ground grid, with noticeable lateral sway. This is evidence of motion feasibility within this simulation, not promotion or physical verification.

Leo requested a longer training window. Five hundred epochs represent only 4,096,000 transitions, and failure at that point does not establish an optimization plateau. Continue the privileged-critic checkpoint to 2,000 total epochs with rewards, commands, actor, terrain and physical model fixed. `DeliveryA2CAgent` now saves the independent critic Adam state, critic counters/LR and actor adaptive LR/entropy state. A two-case CPU test uses the real RL-Games critic: the next critic update is bitwise identical after restoring the new format, and restoring the legacy format explicitly warns about restarting missing critic Adam moments. The epoch-500 checkpoint lacks those moments; they cannot be recovered. Its continuation restores actor/model/critic weights and actor optimizer, recovers actor LR from that optimizer, and starts fresh critic Adam moments once. Future saves retain the complete critic optimizer. This is not claimed to be an uninterrupted 2,000-epoch comparison.

The field named `mean_foot_slip` is still the existing stance-foot-link COM speed proxy. It is not direct contact-point sliding speed, because each foot link includes a moving lower-leg segment. The acceptance threshold is unchanged. If a candidate tracks motion well but fails only this proxy, inspect actual contact-point motion before attributing that failure to sliding.

The capture identifies effective encoder response under its recorded load, not continuous torque limits, contact geometry, ground truth body velocity or voltage dependence. The knee/hip coordinate relationship is retained; motor housing attachment remains a mechanical question before changing generalized torque coupling. Convex contact fidelity must be compared with the preserved SDF model. Real-world walking is unverified until the supervised physical test.

Research and historical intent: see [the review](SIM-TO-REAL-REVIEW-20260904.md). The approach follows measured actuator and delay modeling in [Tan et al.](https://arxiv.org/abs/1804.10332) and [Hwangbo et al.](https://arxiv.org/abs/1901.08652), with bounded parallel PPO and curriculum principles from [Rudin et al.](https://arxiv.org/abs/2109.11978). These papers motivate the method; they do not establish transfer for this robot.

The longer continuation started as run `20260905T183528Z-train-1480`, experiment `2026-09-05_18-35-43`, from the rejected epoch-500 actor for further optimization only. Its immutable source comparison differs only in the trainer entry point and the added checkpoint subclass; all actor/task/reward/configuration, profile, fit, asset and execution-environment hashes match. Epoch 525 saved successfully. CPU inspection of that actual training checkpoint verified eight nonempty, finite critic Adam parameter states, critic epoch/frame 525/4,300,800 and saved actor LR 0.00050625. SHA: `6761415a6b5da02ce4d9d6f6b2d9ab92278174018f01b1e69b34c901f03f0337`. The bounded `evaluate-privileged2000.py` supervisor (PID 610259 at launch) records training progress and launches the fixed flat evaluation after epoch 2,000 completes. Its deadline is 24,000 seconds; evaluation remains bounded separately. Resume coarse monitoring after this successful startup/save verification.
